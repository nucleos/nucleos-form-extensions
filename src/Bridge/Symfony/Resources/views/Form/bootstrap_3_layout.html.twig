{% extends 'bootstrap_3_layout.html.twig' %}

{% block help %}
{% apply spaceless %}
{% if help is defined and help %}
    <p class="help-block">{{ help_translation_domain is same as(false) ? help : help|trans({}, help_translation_domain) }}</p>
{% endif %}
{% endapply %}
{% endblock %}

{% block form_widget -%}
    {{ parent() }}
    {{ block('help') }}
{%- endblock form_widget %}

